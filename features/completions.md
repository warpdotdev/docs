# Completions

## What is it

Tab Completions will suggest commands, option names, path parameters for you.

This feature works regardless of machine or directory (including SSH sessions).&#x20;

## How to access it

Type out the beginning of your command, then press `TAB`.

To search for options and flags, you first need to type `-` then press `TAB`.

## Try it yourself!

1. Type `git checkout` (note the space) and then press `TAB`
2. A menu will show all of your local branches. You can select one using your mouse or the `UP ↑`/`DOWN ↓` arrow keys

## How it works

Refer to the demo below.

{% embed url="https://www.loom.com/share/b4eb2bece5ea4065a5c83cf65297a670" %}
Completions Demo
{% endembed %}
